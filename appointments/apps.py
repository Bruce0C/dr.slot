from django.apps import AppConfig


class AppointmentsConfig(AppConfig):
    """ App configuration for the appointments app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'appointments'
